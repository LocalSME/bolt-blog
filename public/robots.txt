# Served at https://localsme.bolt.host/robots.txt (see CLAUDE.md). That domain is a
# placeholder — this fork has not itself been imported into Bolt.new or published.
# Assumes a root-served host; under a sub-path deployment this file would need to
# move accordingly, or it is ignored.

User-agent: *
Allow: /
Disallow: /admin/

Sitemap: https://localsme.bolt.host/sitemap-index.xml
